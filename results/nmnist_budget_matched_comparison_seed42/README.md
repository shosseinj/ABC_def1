# Invalid Prior Budget-Matched Results

**WARNING: Do not use these files as scientific evidence.**

This directory is retained for provenance but is invalid. Its runner used an
arbitrary backward-pass conversion, did not preserve the required four-generation
TEMP search in matched conditions, calibrated on an evaluation-manifest sample,
and did not provide the required synchronized timing or event-level audit trail.

Use `results/nmnist_budget_matched_comparison_v2_seed42` after its status is
`complete` or explicitly wall-clock-unmatched and its audit has passed.
